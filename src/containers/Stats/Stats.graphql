query getUserStats($account: String) {
  user(where: { address: { _eq: $account } }) {
    maxCollateral
    liquidationRatio
    liquidationCollateral
    borrowRatio
    netApy
    totalSupplyUsd
    totalBorrowUsd
  }
}
