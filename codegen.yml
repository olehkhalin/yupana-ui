overwrite: true
schema: "https://yupana-indexer.stage.madfish.xyz/v1/graphql"
documents: "src/**/*.graphql"
generates:
  src/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
  ./graphql.schema.json:
    plugins:
      - "introspection"
